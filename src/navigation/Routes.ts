import React from 'react';

import Home from '../screens/Home';
import FlightDetails from '../screens/FlightDetails';

// Screen imports

/**
 * NOTE: Page Names Should not have any SPACES, SPECIAL CHARS, ETC.
 */
// page names
export const pageNameHome = 'Home';
export const pageNameFlightDetails = 'FlightDetails';

// page components
export const pageHome = Home;
export const pageFlightDetails = FlightDetails;
